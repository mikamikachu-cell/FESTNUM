 
{# templates/front/email/register.html.twig #}
{{ 'registration.email.message %confirmation_url%'
    | trans({
        '%user%': user,
        '%confirmation_url%': confirmation_url,
        '%host%': app.request.schemeAndHttpHost,
        '%website_name%': website_name
        }, 'security')
    |raw|nl2br
}}