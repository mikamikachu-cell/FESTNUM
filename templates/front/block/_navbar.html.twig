{# <nav>
    navbar à construire
    <img src="{{ asset('ressources/avatar_nicolas.PNG') }}" alt="" srcset="">
</nav> #}

{# NAVBAR ÉCRAN LARGE #}
<nav class="navbar bg-light d-none d-md-block">
    <div class="container">
        <a href="{{ path('front_home') }}">
            <img src="{{ asset('ressources/logo-f.png') }}" alt="logo">
        </a>
        <a href="{{ path('front_home') }}">Accueil</a>
        {# <a href="{{ path('front_videos') }}">Vidéos</a> #}
        <a href="{{ path('front_contact') }}">Contact</a>
        <a href="{{ path('front_jury') }}">Jury</a>
        <a href="{{ path('front_archives') }}">Archives</a>
        <a href="{{ path('front_prix') }}">prix du festival</a>
        <div class="dropdown">
            <a class="btn btn-light dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                data-bs-toggle="dropdown" aria-expanded="false">
                Mon compte
            </a>
            {% if app.user %}
            <!-- Si utilisateur connecté -->
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                <li>
                    <a class="dropdown-item" href="{{ path('front_profile') }}">
                        <i class="fas fa-user"></i> Mon profil
                    </a>
                </li>
                <li>
                    <a class="dropdown-item" href="{{ path('submit_project', {id: app.user.id}) }}">
                        <i class="fas fa-file-upload"></i> Soumettre mon film
                    </a>
                </li>
                <li>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">
                        <i class="fas fa-sign-out-alt"></i> Me déconnecter
                    </a>
                </li>
            </ul>
            {% else %}
            <!-- Si utilisateur non connecté -->
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                <li><a class="dropdown-item" href="{{ path('app_login') }}">Me connecter</a></li>
                <li><a class="dropdown-item" href="{{ path('front_register') }}">M'enregistrer</a></li>
            </ul>
            {% endif %}
        </div>
    </div>
</nav>

{# NAVBAR ÉCRAN MOBILE #}
<nav class="navbar navbar-light bg-light p-0 d-md-none">
    <div class="container-fluid mx-0 px-0">
        <a class="navbar-brand m-1" href="{{ path('front_home') }}"><img class="mx-2"
                src="{{ asset('ressources/logo-f.png') }}" alt="logo"></a>
        <button class="navbar-toggler mx-3" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav text-center bg-light">
                <li class="navitem border-bottom">
                    <a class="nav-link" href="{{ path('front_home') }}">Accueil</a>
                </li>
                {# <li class="navitem border-bottom">
                    <a class="nav-link" href="{{ path('front_videos') }}">Vidéos</a>
                </li> #}
                <li class="navitem border-bottom">
                    <a class="nav-link" href="{{ path('front_contact') }}">Contact</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="{{ path('front_jury') }}">Jury</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="{{ path('front_archives') }}">Archives</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="{{ path('front_prix') }}">Prix</a>
                </li>

                <li class="dropdown navitem border-bottom">
                    <a class="btn btn-light dropdown-toggle nav-link" href="#" role="button" id="dropdownMenuLink"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Mon compte
                    </a>
                    {% if app.user %}
                    <!-- Si utilisateur connecté -->
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <li class="navitem border-bottom">
                            <a class="dropdown-item nav-link w-50 m-auto" href="{{ path('front_profile') }}">
                                <i class="fas fa-user"></i> Mon profil
                            </a>
                        </li>
                        <li class="navitem border-bottom">
                            <a class="dropdown-item nav-link w-50 m-auto"
                                href="{{ path('submit_project', {id: app.user.id}) }}">
                                <i class="fas fa-file-upload"></i> Soumettre mon film
                            </a>
                        </li>
                        <li class="navitem">
                            <a class="dropdown-item nav-link w-50 m-auto" href="{{ path('app_logout') }}">
                                <i class="fas fa-sign-out-alt"></i> Me déconnecter
                            </a>
                        </li>
                    </ul>
                    {% else %}
                    <!-- Si utilisateur non connecté -->
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <li class="navitem border-bottom">
                            <a class="dropdown-item nav-link w-50 m-auto" href="{{ path('app_login') }}">Me
                                connecter</a>
                        </li>
                        <li class="navitem">
                            <a class="dropdown-item nav-link w-50 m-auto"
                                href="{{ path('front_register') }}">M'enregistrer</a>
                        </li>
                    </ul>
                    {% endif %}
                </li>

            </ul>
        </div>
    </div>
</nav>


{# NAVBAR avec un seul onglet 'Mon compte' : pour le développement des fonctionnalités back par Alexis #}
{# <div class="navbar navbar-expand-md navbar-dark bg-dark mb-4" role="navigation">
    <a class="navbar-brand" href="#">Festnum</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
        aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item{# {{ route == 'front_home' ? ' active' }}">
                <a class="nav-link" href="{{ path('front_home') }}">Home</a>
            </li>

            <div class="dropdown">
                <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                    data-bs-toggle="dropdown" aria-expanded="false">
                    Mon compte
                </a>
                {% if app.user %}
                <!-- Si utilisateur connecté -->
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <li>
                        <a class="dropdown-item" href="{{ path('front_profile') }}">
                            <i class="fas fa-user"></i> Mon profil
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">
                            <i class="fas fa-sign-out-alt"></i> Me déconnecter
                        </a>
                    </li>
                </ul>
                {% else %}
                <!-- Si utilisateur non connecté -->
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <li><a class="dropdown-item" href="{{ path('app_login') }}">Me connecter</a></li>
                    <li><a class="dropdown-item" href="{{ path('front_register') }}">M'enregistrer</a></li>
                </ul>
                {% endif %}
            </div>



            {% if app.user %}
            <!-- Si utilisateur connecté -->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="dropdown_account" data-toggle="dropdown" aria-haspopup="true"
                    aria-expanded="false">
                    <i class="fas fa-user"></i> Mon compte
                </a>
                <ul class="dropdown-menu" aria-labelledby="dropdown_account">
                    {% if is_granted("ROLE_ADMIN") %}
                    <li class="dropdown-item">
                        <a href="{{ path('back_home') }}">
                            <i class="fas fa-user-cog"></i> Mon profil (admin)
                        </a>
                    </li>
                    {% else %}
                    <li class="dropdown-item">
                        <a href="{{ path('front_profile') }}">
                            <i class="fas fa-user-cog"></i> Mon profil
                        </a>
                    </li>
                    {% endif %}
                    {# <li class="dropdown-item {{ route == 'app_reset_password' ? ' active' }}">
                        <a href="{{ path('app_reset_password', { 'id': app.user.id }) }}">
                            <i class="fas fa-key"></i> Modifier mon mot de passe
                        </a>
                    </li>
                    <li class="dropdown-item">
                        <a href="{{ path('app_logout') }}">
                            <i class="fas fa-sign-out-alt"></i> Me déconnecter
                        </a>
                    </li>
                </ul>
            </li>
            {% else %}
            <!-- Si utilisateur non connecté -->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="dropdown_account" data-toggle="dropdown" aria-haspopup="true"
                    aria-expanded="false">
                    <i class="fas fa-user"></i> Mon compte
                </a>
                <ul class="dropdown-menu" aria-labelledby="dropdown_account">
                    <li class="dropdown-item{#{{ route == 'app_login' ? ' active' }}">
                        <a href="{{ path('app_login') }}">
                            <i class="fas fa-sign-in-alt"></i> Me connecter
                        </a>
                    </li>
                    <li class="dropdown-item{#{{ route == 'front_register' ? ' active' }}">
                        <a href="{{ path('front_register') }}">
                            <i class="fas fa-user"></i> M'enregistrer
                        </a>
                    </li>
                </ul>
            </li>
            {% endif %}
        </ul>
    </div>
</div> #}

{# NAVBAR ECRAN LARGE #}
{# <nav class="navbar bg-light d-none d-md-block">
    <div class="container">
        <img src="{{ asset('ressources/logo-f.png') }}" alt="logo">
        <a href="accueil.html">Accueil</a>
        <a href="video.html">Vidéos</a>
        <a href="contact.html">Contact</a>
        <a href="jury.html">Jury</a>
        <a href="archive.html">Archives</a>
        <a href="prix.html">prix du festival</a>
        {% if app.user %}
        {#
        <a href="#">Mon compte</a>
        {% else %}
        <a href="{{ path('app_login') }}">Mon compte</a>
        {% endif %} #}


        {# ANCICENNE VERSION | NAVBAR ECRAN LARGE #}
        {# <ul class="navbar-nav mr-auto">
            {% if app.user %}
            <!-- Si utilisateur connecté -->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="dropdown_account" data-toggle="dropdown" aria-haspopup="true"
                    aria-expanded="false">
                    Mon compte
                </a>
                <ul class="dropdown-menu" aria-labelledby="dropdown_account">
                    <li class="dropdown-item">
                        <a href="#">Profil</a>
                    </li>
                    <li class="dropdown-item">
                        <a href="{{ path('app_logout') }}">Me déconnecter</a>
                    </li>
                    {# <li class="dropdown-item{{ route == 'app_reset_password' ? ' active' }}">
                        <a href="{{ path('app_reset_password', { 'id': app.user.id }) }}">
                            <i class="fas fa-"></i> {{ 'nav.account_reset_password'|trans() }}
                        </a>
                    </li>
                </ul>
            </li>
            {% else %}
            <!-- Si utilisateur non connecté -->
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="dropdown_account" data-toggle="dropdown" aria-haspopup="true"
                    aria-expanded="false">
                    Mon compte
                </a>
                <ul class="dropdown-menu" aria-labelledby="dropdown_account">
                    <li class="dropdown-item{{ route == 'app_login' ? ' active' }}">
                        <a href="{{ path('app_login') }}">Me connecter</a>
                    </li>
                    <li class="dropdown-item{{ route == 'front_register' ? ' active' }}">
                        <a href="{{ path('front_register') }}">M'enregistrer</a>
                    </li>
                </ul>
            </li>
            {% endif %}
        </ul>
    </div>
</nav> #}

{# NAVBAR MOBILE #}
{# <nav class="navbar navbar-light bg-light p-0 d-md-none">
    <div class="container-fluid mx-0 px-0">
        <a class="navbar-brand m-1" href="#">
            <img class="mx-2" src="{{ asset('ressources/logo-f.png') }}" alt="logo">
        </a>
        <button class="navbar-toggler mx-3" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav text-center bg-light">
                <li class="navitem border-bottom">
                    <a class="nav-link active" aria-current="page" href="#">Accueil</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="video.html">Vidéos</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="contact.html">Contact</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="jury.html">jury</a>
                </li>
                <li class="navitem border-bottom">
                    <a class="nav-link" href="archive.html">archives</a>
                </li>
                <li class="navitem ">
                    <a class="nav-link" href="prix.html">prix</a>
                </li> #}
                {# {% if app.user %}
                <li class="navitem ">
                    <a href="{{ path('app_logout') }}" class="nav-link" id="dropdown_account" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Mon compte
                    </a>
                </li>
                {% else %}
                <li class="navitem ">
                    <a href="{{ path('app_logout') }}" class="nav-link" id="dropdown_account" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Mon compte
                    </a>
                </li>
                {% endif %} #}



                {# ANCIENNE VERSION | NAVBAR MOBILE #}
                {# {% if app.user %}
                <!-- Si utilisateur connecté -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="dropdown_account" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Mon compte
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="dropdown_account">
                        <li class="dropdown-item">
                            <a href="#">Profil</a>
                        </li>
                        <li class="dropdown-item">
                            <a href="{{ path('app_logout') }}">Me déconnecter</a>
                        </li>
                        {# <li class="dropdown-item{{ route == 'app_reset_password' ? ' active' }}">
                            <a href="{{ path('app_reset_password', { 'id': app.user.id }) }}">
                                <i class="fas fa-"></i> {{ 'nav.account_reset_password'|trans() }}
                            </a>
                        </li>
                    </ul>
                </li>
                {% else %}
                <!-- Si utilisateur non connecté -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="dropdown_account" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Mon compte
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="dropdown_account">
                        <li class="dropdown-item{{ route == 'app_login' ? ' active' }}">
                            <a href="{{ path('app_login') }}">Me connecter</a>
                        </li>
                        <li class="dropdown-item{{ route == 'front_register' ? ' active' }}">
                            <a href="{{ path('front_register') }}">M'enregistrer</a>
                        </li>
                    </ul>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
#}